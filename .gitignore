# Dependency and build outputs
node_modules/
dist/
coverage/

# Local environment/configuration
.env
.env.*
!.env.example

# Session continuity handoff is local working state, not source history
.loom/config.yaml
.loom/handoff.md
.loom/session-smoke.txt

# Flattened starter references kept locally during bootstrap
src_agents_base.ts
src_index.ts
src_pipeline_types.ts
src_tools_base.ts

# Generated reference outputs; canonical spec remains SPEC.md
SPEC-with-manifests.md
