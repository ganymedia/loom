name: LOOM
version: 0.1.1
created: 2026-07-07
updated: 2026-08-13
current_phase: phase-8
current_milestone: m8-1
current_task: t8-1-3
phases:
  - id: phase-1
    name: Foundation
    description: "Core infrastructure: config, backends, CLI, basic TUI, and file tools"
    exit_criterion: loom opens, talks to a real vLLM endpoint, and reads/writes real files
    status: completed
    token_estimate: large
    milestones:
      - id: milestone-1-1
        name: Config System
        description: Zod schema and layered config loader (global → project → env)
        status: completed
        token_estimate: medium
        tasks:
          - id: task-1-1-1
            name: Config schema
            description: Zod schemas for backend, profile, and full LOOM config
            status: completed
            token_estimate: small
            files:
              - src/config/schema.ts
              - tests/config/schema.test.ts
            dependencies: []
          - id: task-1-1-2
            name: Config loader
            description: Layered config loader with global, project, and env overrides
            status: completed
            token_estimate: medium
            files:
              - src/config/loader.ts
              - tests/config/loader.test.ts
            dependencies: []
      - id: milestone-1-2
        name: Backend Discovery
        description: Model discovery and backend routing
        status: completed
        token_estimate: medium
        tasks:
          - id: task-1-2-1
            name: Model discovery
            description: GET /v1/models endpoint discovery with error handling
            status: completed
            token_estimate: medium
            files:
              - src/backends/discovery.ts
              - tests/backends/discovery.test.ts
            dependencies: []
          - id: task-1-2-2
            name: Backend router
            description: Backend key resolution and model selection
            status: completed
            token_estimate: small
            files:
              - src/backends/router.ts
              - tests/backends/router.test.ts
            dependencies: []
      - id: milestone-1-3
        name: CLI Skeleton
        description: Commander.js setup with subcommand registration
        status: completed
        token_estimate: medium
        tasks:
          - id: task-1-3-1
            name: CLI entry point
            description: Thin index.ts with config loading and subcommand registration
            status: completed
            token_estimate: small
            files:
              - src/index.ts
            dependencies: []
          - id: task-1-3-2
            name: Config subcommand
            description: loom config command for inspecting configuration
            status: completed
            token_estimate: small
            files:
              - src/cli/commands/config.ts
            dependencies: []
      - id: milestone-1-4
        name: Developer Agent and File Tools
        description: Developer agent with file read/write tools and path safety
        status: completed
        token_estimate: large
        tasks:
          - id: task-1-4-1
            name: File read/write tools
            description: File reader and writer tools with path safety boundaries
            status: completed
            token_estimate: medium
            files:
              - src/tools/file-reader.ts
              - src/tools/file-writer.ts
              - src/tools/path-safety.ts
              - tests/tools/file-reader.test.ts
              - tests/tools/file-writer.test.ts
            dependencies: []
          - id: task-1-4-2
            name: Developer agent
            description: Developer agent with tool call parsing and execution
            status: completed
            token_estimate: large
            files:
              - src/agents/developer.ts
              - tests/agents/developer.test.ts
            dependencies: []
          - id: task-1-4-3
            name: Shell tool
            description: Shell execution tool with path safety and command restrictions
            status: completed
            token_estimate: medium
            files:
              - src/tools/shell.ts
              - tests/tools/shell.test.ts
            dependencies: []
          - id: task-1-4-4
            name: Git operations tool
            description: Git operations tool with path safety and command restrictions
            status: completed
            token_estimate: medium
            files:
              - src/tools/git-ops.ts
              - tests/tools/git-ops.test.ts
            dependencies: []
      - id: milestone-1-5
        name: Basic TUI
        description: TUI smoke test, single-turn execution, and follow-up loop
        status: completed
        token_estimate: medium
        tasks:
          - id: task-1-5-1
            name: TUI smoke test
            description: Session smoke test for backend and file operations
            status: completed
            token_estimate: medium
            files:
              - src/tui/session.ts
              - tests/tui/session.test.ts
            dependencies: []
          - id: task-1-5-2
            name: Single-turn prompt execution
            description: --prompt flag for single Developer agent turn
            status: completed
            token_estimate: small
            files:
              - src/tui/session.ts
            dependencies: []
          - id: task-1-5-3
            name: Interactive TUI loop
            description: Follow-up turns and visible tool-call results in the TUI/session
              path
            status: completed
            token_estimate: large
            files:
              - src/tui/session.ts
              - tests/tui/session.test.ts
            dependencies: []
  - id: phase-2
    name: Core Features
    description: Prompt Store, Session Continuity, and remaining built-in agents
    exit_criterion: Prompt Store, Session Continuity, built-in agents, and agent tab
      switching work
    status: completed
    token_estimate: xlarge
    milestones:
      - id: milestone-2-1
        name: Prompt Store
        description: SQLite-backed prompt history and recall
        status: completed
        token_estimate: large
        tasks:
          - id: task-2-1-1
            name: Prompt Store schema
            description: SQLite schema for prompt history and embeddings
            status: completed
            token_estimate: medium
            files:
              - src/store/schema.ts
            dependencies: []
          - id: task-2-1-2
            name: Prompt Store implementation
            description: SQLite operations for storing and retrieving prompts
            status: completed
            token_estimate: large
            files:
              - src/store/prompt-store.ts
              - tests/store/prompt-store.test.ts
            dependencies: []
      - id: milestone-2-2
        name: Session Continuity
        description: Token tracking and automatic handoff generation
        status: completed
        token_estimate: large
        tasks:
          - id: task-2-2-1
            name: Session Manager
            description: Token tracking and handoff trigger at 80% threshold
            status: completed
            token_estimate: large
            files:
              - src/session/manager.ts
              - tests/session/manager.test.ts
            dependencies: []
          - id: task-2-2-2
            name: Handoff document management
            description: Read/write handoff.md and narrative.md
            status: completed
            token_estimate: medium
            files:
              - src/session/handoff.ts
              - tests/session/handoff.test.ts
            dependencies: []
      - id: milestone-2-3
        name: Additional Built-in Agents
        description: Architect, Tester, and Security agents
        status: completed
        token_estimate: xlarge
        tasks:
          - id: task-2-3-1
            name: Architect agent
            description: Architect agent for high-level design and planning
            status: completed
            token_estimate: large
            files:
              - src/agents/architect.ts
              - tests/agents/architect.test.ts
            dependencies: []
          - id: task-2-3-2
            name: Tester agent
            description: Tester agent for test generation and execution
            status: completed
            token_estimate: large
            files:
              - src/agents/tester.ts
              - tests/agents/tester.test.ts
            dependencies: []
          - id: task-2-3-3
            name: Security agent
            description: Security agent for code review and vulnerability detection
            status: completed
            token_estimate: large
            files:
              - src/agents/security.ts
              - tests/agents/security.test.ts
            dependencies: []
      - id: milestone-2-4
        name: Agent Tab Switching
        description: TUI tab strip for switching between agents
        status: completed
        token_estimate: medium
        tasks:
          - id: task-2-4-1
            name: Agent tab switching
            description: Session agent switching and tab strip formatting
            status: completed
            token_estimate: medium
            files:
              - src/tui/tab-strip.ts
              - tests/tui/tab-strip.test.ts
              - src_cli_commands_theme.ts
              - src_tui_components.tsx
              - src_tui_theme.ts
            dependencies: []
  - id: phase-3
    name: Intelligence
    description: Prompt Intelligence pipeline and Project Planner
    exit_criterion: Prompt Intelligence and loom plan commands work and are verified
    status: completed
    token_estimate: xlarge
    milestones:
      - id: milestone-3-1
        name: Prompt Intelligence Pipeline
        description: Assembly → budget → optimization → backend request → validation → retry
        status: completed
        token_estimate: xlarge
        tasks:
          - id: task-3-1-1
            name: Prompt assembly
            description: Context assembly and token budget management
            status: completed
            token_estimate: large
            files:
              - src/prompt/assembly.ts
              - tests/prompt/assembly.test.ts
            dependencies: []
          - id: task-3-1-2
            name: Prompt optimization
            description: Context compression and prompt optimization
            status: completed
            token_estimate: large
            files:
              - src/prompt/optimization.ts
              - tests/prompt/optimization.test.ts
            dependencies: []
          - id: task-3-1-3
            name: Retry loop
            description: Structured retry with exponential backoff
            status: completed
            token_estimate: medium
            files:
              - src/prompt/retry.ts
              - tests/prompt/retry.test.ts
            dependencies: []
      - id: milestone-3-2
        name: Project Planner
        description: plan.yaml schema and loom plan * commands
        status: completed
        token_estimate: large
        tasks:
          - id: task-3-2-1
            name: Plan.yaml schema
            description: Zod schema for plan.yaml structure
            status: completed
            token_estimate: medium
            files:
              - src/planner/schema.ts
              - tests/planner/schema.test.ts
            dependencies: []
          - id: task-3-2-2
            name: Plan parser
            description: Plan.yaml parsing and validation
            status: completed
            token_estimate: medium
            files:
              - src/planner/parser.ts
              - tests/planner/parser.test.ts
            dependencies: []
          - id: task-3-2-3
            name: Plan commands
            description: loom plan * subcommands (decompose, status, etc.)
            status: completed
            token_estimate: large
            files:
              - src/cli/commands/plan.ts
              - src/planner/operations.ts
              - src/planner/writer.ts
              - tests/cli/commands/plan.test.ts
              - tests/planner/operations.test.ts
            dependencies: []
  - id: phase-4
    name: Platform
    description: Pipeline engine, sub-agent runtime, Foundry client, and recall/log commands
    exit_criterion: Pipeline engine, sub-agent runtime, Foundry client, recall
      command, and log command work
    status: completed
    token_estimate: xlarge
    milestones:
      - id: milestone-4-1
        name: Pipeline Engine
        description: .loom YAML parser and stage executors
        status: completed
        token_estimate: xlarge
        tasks:
          - id: task-4-1-1
            name: Pipeline parser
            description: .loom YAML parsing and validation
            status: completed
            token_estimate: large
            files:
              - src/pipeline/parser.ts
              - tests/pipeline/parser.test.ts
            dependencies: []
          - id: task-4-1-2
            name: Stage executors
            description: Five stage executors (prompt, transform, branch, parallel, inject)
            status: completed
            token_estimate: xlarge
            files:
              - src/pipeline/executors/prompt.ts
              - src/pipeline/executors/transform.ts
              - src/pipeline/executors/branch.ts
              - src/pipeline/executors/parallel.ts
              - src/pipeline/executors/inject.ts
              - tests/pipeline/executors/*.test.ts
            dependencies: []
          - id: task-4-1-3
            name: DAG walker
            description: Pipeline execution with dependency resolution
            status: completed
            token_estimate: large
            files:
              - src/pipeline/dag-walker.ts
              - tests/pipeline/dag-walker.test.ts
            dependencies: []
      - id: milestone-4-2
        name: Sub-agent Runtime
        description: Sub-agent spawning and lifecycle management
        status: completed
        token_estimate: large
        tasks:
          - id: task-4-2-1
            name: Sub-agent runner
            description: Sub-agent spawning and context passing
            status: completed
            token_estimate: large
            files:
              - src/agents/sub-agent-runner.ts
              - tests/agents/sub-agent-runner.test.ts
            dependencies: []
      - id: milestone-4-3
        name: Foundry Client
        description: Foundry HTTP client for publish/install/search
        status: completed
        token_estimate: large
        tasks:
          - id: task-4-3-1
            name: Foundry HTTP client
            description: HTTP client for Foundry package registry
            status: completed
            token_estimate: large
            files:
              - src/foundry/client.ts
              - tests/foundry/client.test.ts
            dependencies: []
      - id: milestone-4-4
        name: Recall and Log Commands
        description: Prompt history recall and logging commands
        status: completed
        token_estimate: medium
        tasks:
          - id: task-4-4-1
            name: Recall command
            description: Cosine-similarity recall from Prompt Store
            status: completed
            token_estimate: medium
            files:
              - src/cli/commands/recall.ts
              - tests/cli/commands/recall.test.ts
            dependencies: []
          - id: task-4-4-2
            name: Log command
            description: Prompt history logging and retrieval
            status: completed
            token_estimate: medium
            files:
              - src/cli/commands/log.ts
              - tests/cli/commands/log.test.ts
            dependencies: []
  - id: phase-5
    name: Verification, Design System, and Production Readiness
    description: Close gaps between completed Phase 1-4 plan tasks and the full LOOM
      specification, including claim audit, missing references, Ink TUI,
      embeddings, live handoff verification, and production packaging.
    exit_criterion: A single compiled loom binary exists, opens an Ink-rendered TUI
      matching the loom-dark theme, performs real semantic recall end-to-end
      from a natural-language query, has a verified Foundry client against a
      confirmed API spec, and has zero uncommitted or unverified work.
    status: completed
    token_estimate: xlarge
    milestones:
      - id: m5-1
        name: Repository and claim audit
        description: Confirm every narrative claim about implemented and verified work
          against actual committed, tested code.
        status: completed
        token_estimate: medium
        tasks:
          - id: t5-1-1
            name: Run git status; commit or explicitly document any uncommitted work
            description: Establish the repository state before Phase 5 work; uncommitted
              operator-provided planning docs must be committed or explicitly
              documented before implementation continues.
            status: completed
            token_estimate: small
            files:
              - .loom/narrative.md
            dependencies: []
          - id: t5-1-2
            name: Confirm whether foundry-api.md existed in the working tree when
              src/foundry/ was built
            description: If foundry-api.md was absent when the Foundry client was written,
              treat every endpoint shape, auth header, and error envelope in
              src/foundry/ as unverified against spec rather than trusting tests
              written against the same assumptions.
            status: completed
            token_estimate: small
            files:
              - foundry-api.md
              - src/foundry/client.ts
              - tests/foundry/client.test.ts
            dependencies: []
          - id: t5-1-3
            name: Reconcile SPEC.md section 7 against the current repo tree one more time
            description: Verify the exact reference-spec manifest in SPEC.md section 7
              against actual repository paths and flag mismatches as spec/repo
              drift.
            status: completed
            token_estimate: small
            files:
              - SPEC.md
              - support_docs/SPEC.md
            dependencies:
              - t5-1-1
          - id: t5-1-4
            name: Audit which plan.yaml tasks 1-34 map to which real deliverable
            description: Map prior plan tasks to real deliverables and flag broad task names
              whose implementation was narrower than the original requirement,
              especially the text tab strip vs Ink TUI scope reduction.
            status: completed
            token_estimate: medium
            files:
              - .loom/plan.yaml
              - .loom/narrative.md
            dependencies: []
      - id: m5-2
        name: Add missing reference specs to repository
        description: Ensure Foundry, config, and agent test reference specs exist at the
          repository root and re-verify Foundry against the now-present API
          reference.
        status: completed
        token_estimate: medium
        tasks:
          - id: t5-2-1
            name: Add foundry-api.md to repository root
            description: Add the Foundry REST API reference at the root; request it from the
              operator if not available locally.
            status: completed
            token_estimate: small
            files:
              - foundry-api.md
            dependencies: []
          - id: t5-2-2
            name: Add foundry-server.toml to repository root
            description: Add the self-hosted Foundry server configuration reference at the
              root.
            status: completed
            token_estimate: small
            files:
              - foundry-server.toml
            dependencies: []
          - id: t5-2-3
            name: Add loom-config.yaml to repository root
            description: Add the global LOOM config example reference at the root.
            status: completed
            token_estimate: small
            files:
              - loom-config.yaml
            dependencies: []
          - id: t5-2-4
            name: Add loom-tests.yaml to repository root
            description: Add the agent test suite format reference at the root.
            status: completed
            token_estimate: small
            files:
              - loom-tests.yaml
            dependencies: []
          - id: t5-2-5
            name: Re-verify src/foundry/ client against foundry-api.md once present; fix any
              drift
            description: Compare the Foundry client implementation and tests against the
              root Foundry API reference and fix any endpoint, auth, or
              error-envelope drift.
            status: completed
            token_estimate: medium
            files:
              - foundry-api.md
              - src/foundry/client.ts
              - tests/foundry/client.test.ts
            dependencies:
              - t5-2-1
      - id: m5-3
        name: Ink TUI and design system integration
        description: Install Ink/React, promote flattened design-system references into
          live modules, and replace the text session renderer with an Ink
          component tree.
        status: completed
        token_estimate: xlarge
        tasks:
          - id: t5-3-1
            name: Add ink, react, and their type dependencies to package.json
            description: Add the runtime and type dependencies required for an Ink-rendered
              TUI.
            status: completed
            token_estimate: small
            files:
              - package.json
              - bun.lock
            dependencies: []
          - id: t5-3-2
            name: Move src_tui_theme.ts to src/tui/theme.ts as the live module
            description: Promote the flattened theme reference into the live TUI module tree
              without treating the reference as executable code.
            status: completed
            token_estimate: small
            files:
              - src/tui/theme.ts
            dependencies: []
          - id: t5-3-3
            name: Move src_tui_components.tsx to src/tui/components.tsx as the live module
            description: Promote the flattened TUI components reference into the live module
              tree after Ink dependencies and theme support exist.
            status: completed
            token_estimate: small
            files:
              - src/tui/components.tsx
            dependencies:
              - t5-3-1
              - t5-3-2
          - id: t5-3-4
            name: Move src_cli_commands_theme.ts to src/cli/commands/theme.ts; register loom
              theme list/use/preview
            description: Promote the flattened theme CLI reference into a registered command
              group.
            status: completed
            token_estimate: small
            files:
              - src/cli/commands/theme.ts
              - src/index.ts
            dependencies:
              - t5-3-2
          - id: t5-3-5
            name: Rebuild src/tui/session.ts as an Ink component tree, replacing the text
              tab strip with AgentTabStrip
            description: Replace the interim text-based tab switching with the originally
              specified Ink UI while preserving conversation history across
              tabs, tool-call display, and deterministic test input mode.
            status: completed
            token_estimate: large
            files:
              - src/tui/session.ts
              - src/tui/components.tsx
              - tests/tui/session.test.ts
            dependencies:
              - t5-3-3
          - id: t5-3-6
            name: Wire StatusBar into the live session loop
            description: Show token progress, session id, and active agent through the live
              StatusBar component.
            status: completed
            token_estimate: medium
            files:
              - src/tui/session.ts
              - src/tui/components.tsx
              - tests/tui/session.test.ts
            dependencies:
              - t5-3-5
          - id: t5-3-7
            name: Add defaults.theme field to config schema
            description: Add a defaults.theme config field with default loom-dark per the
              prior spec note.
            status: completed
            token_estimate: small
            files:
              - src/config/schema.ts
              - tests/config/schema.test.ts
            dependencies:
              - t5-3-4
          - id: t5-3-8
            name: TSX typecheck coverage in tsconfig.json and Biome config
            description: Ensure TSX files are typechecked and linted by the project
              configuration.
            status: completed
            token_estimate: small
            files:
              - tsconfig.json
              - biome.json
            dependencies:
              - t5-3-1
      - id: m5-4
        name: Embedding generation
        description: Add real embedding generation so semantic recall works from
          natural-language queries and pipeline injection can use Context Bus
          state.
        status: completed
        token_estimate: large
        tasks:
          - id: t5-4-1
            name: Implement POST /v1/embeddings client against the configured embedding
              backend
            description: Resolve backend and model from store embedding configuration
              through the same model-discovery seam; do not hardcode model
              names.
            status: completed
            token_estimate: medium
            files:
              - src/config/schema.ts
              - src/backends/embeddings.ts
              - tests/backends/embeddings.test.ts
            dependencies: []
          - id: t5-4-2
            name: Wire embedding generation into Prompt Store writes so every session turn
              is embedded automatically
            description: Embed stored session turns automatically after the embedding client
              exists.
            status: completed
            token_estimate: medium
            files:
              - src/store/prompt-store.ts
              - src/cli/commands/log.ts
              - src/index.ts
              - tests/store/prompt-store.test.ts
            dependencies:
              - t5-4-1
          - id: t5-4-3
            name: Update loom recall to accept a natural-language --query in addition to
              --vector
            description: Keep explicit --vector support and add --query as the primary path
              that embeds text through the embedding client.
            status: completed
            token_estimate: medium
            files:
              - src/cli/commands/recall.ts
              - src/index.ts
              - tests/cli/commands/recall.test.ts
            dependencies:
              - t5-4-1
          - id: t5-4-4
            name: Wire inject pipeline stage to call recall automatically using Context Bus
              state as the query
            description: Use assembled Context Bus state to generate recall queries for
              inject stages.
            status: completed
            token_estimate: medium
            files:
              - src/pipeline/executors/inject.ts
              - tests/pipeline/executors/stage-executors.test.ts
            dependencies:
              - t5-4-3
      - id: m5-5
        name: Confirm automatic handoff triggering in the live session loop
        description: Prove Session Manager and handoff document management are actually
          wired into the live session loop.
        status: completed
        token_estimate: medium
        tasks:
          - id: t5-5-1
            name: Write integration test for automatic handoff past 80 percent token
              threshold
            description: Run a real session loop past the threshold and assert
              .loom/handoff.md is written automatically, not just that Session
              Manager and handoff utilities pass unit tests independently.
            status: completed
            token_estimate: medium
            files:
              - src/tui/session.ts
              - tests/tui/session.test.ts
            dependencies: []
          - id: t5-5-2
            name: Confirm new-session handoff ingestion injects into first agent turn
              context
            description: Verify handoff ingestion affects the first agent turn's context
              rather than only reading the file.
            status: completed
            token_estimate: small
            files:
              - src/tui/session.ts
              - tests/tui/session.test.ts
            dependencies:
              - t5-5-1
      - id: m5-6
        name: Production binary packaging
        description: Compile and smoke-test a standalone LOOM binary after UI, recall,
          and session-continuity work is complete.
        status: completed
        token_estimate: large
        tasks:
          - id: t5-6-1
            name: Run bun build --compile and produce a working dist/loom binary
            description: Build a standalone binary artifact with Bun compile.
            status: completed
            token_estimate: small
            files:
              - package.json
              - dist/loom
            dependencies: []
          - id: t5-6-2
            name: Smoke test the compiled binary standalone against a real configured
              backend
            description: Execute the compiled binary directly rather than through bun run
              and confirm it works with a real configured backend.
            status: completed
            token_estimate: small
            files:
              - dist/loom
            dependencies:
              - t5-6-1
          - id: t5-6-3
            name: Add build:linux and build:mac targets verification
            description: Add or verify platform build targets for Linux and macOS.
            status: completed
            token_estimate: small
            files:
              - package.json
            dependencies:
              - t5-6-1
          - id: t5-6-4
            name: Full regression pass
            description: Run bun run typecheck, bun run lint, bun test, plus one live
              end-to-end session covering agent switching, a pipeline run, a
              plan command, and a handoff.
            status: completed
            token_estimate: large
            files:
              - .loom/narrative.md
            dependencies:
              - t5-6-2
  - id: phase-6
    name: CLI polish, pipeline command, and installer-style release
    description: Close CLI UX, public pipeline command, config-reference, installer,
      and user-testing gaps before external testing.
    exit_criterion: A stranger with no repository access can download a released LOOM
      binary, install it with a single documented command, configure it against their
      own backend, and complete the full user testing guide without hitting an
      undocumented failure.
    status: completed
    token_estimate: xlarge
    milestones:
      - id: m6-1
        name: Fix --version and --help fallthrough
        description: ./dist/loom --version prints a version string and exits 0;
          ./dist/loom --help prints help and exits 0 with no wrapper error.
        status: completed
        token_estimate: medium
        tasks:
          - id: t6-1-1
            name: Fix --version to print version and exit before session startup runs
            description: Commander.js's built-in --version should short-circuit before
              config loading or session bootstrap; confirm the entry point is not
              intercepting that exit path.
            status: completed
            token_estimate: small
            files:
              - src/index.ts
              - tests/cli
            dependencies: []
          - id: t6-1-2
            name: Fix --help fatal wrapper error after help text prints
            description: Help text currently prints correctly, then a fatal wrapper
              message follows; fix the top-level error-handling boundary without
              suppressing Commander's intentional exit.
            status: completed
            token_estimate: small
            files:
              - src/index.ts
              - tests/cli
            dependencies: []
          - id: t6-1-3
            name: Add a CLI smoke test asserting exit codes for --version and --help
            description: Add a CLI smoke test asserting successful exit codes and no
              fatal wrapper output for --version and --help.
            status: completed
            token_estimate: small
            files:
              - tests/cli
            dependencies:
              - t6-1-1
              - t6-1-2
      - id: m6-2
        name: Register loom pipeline run as a public command
        description: Expose loom pipeline run <file.loom> through the CLI using the
          existing parser, DAG walker, stage executors, and branch-skipping behavior.
        status: completed
        token_estimate: large
        tasks:
          - id: t6-2-1
            name: Create src/cli/commands/pipeline.ts wrapping the existing pipeline engine module
            description: Build a thin CLI wrapper only; do not reimplement the already
              verified pipeline parser, DAG walker, or stage executors.
            status: completed
            token_estimate: medium
            files:
              - src/cli/commands/pipeline.ts
              - tests/cli/commands/pipeline.test.ts
            dependencies: []
          - id: t6-2-2
            name: Register the pipeline command group in src/index.ts
            description: Register the public pipeline command group in the thin CLI
              entry point.
            status: completed
            token_estimate: small
            files:
              - src/index.ts
            dependencies:
              - t6-2-1
          - id: t6-2-3
            name: Support --var and --input flags for variable injection into the Context Bus
            description: Allow users to inject variables into the pipeline Context Bus
              through explicit --var and --input CLI flags.
            status: completed
            token_estimate: small
            files:
              - src/cli/commands/pipeline.ts
              - tests/cli/commands/pipeline.test.ts
            dependencies:
              - t6-2-1
          - id: t6-2-4
            name: Add a CLI integration test running a real .loom pipeline file end-to-end through the new command
            description: Run a real .loom pipeline file end-to-end through the public
              pipeline command and verify DAG behavior from the CLI.
            status: completed
            token_estimate: medium
            files:
              - tests/cli/commands/pipeline.test.ts
            dependencies:
              - t6-2-2
              - t6-2-3
      - id: m6-3
        name: Reconcile config reference docs with actual YAML runtime
        description: Every reference document describing LOOM's config format shows
          YAML matching src/config/schema.ts exactly; no user-facing TOML config
          example remains.
        status: completed
        token_estimate: medium
        tasks:
          - id: t6-3-1
            name: Replace loom-config.toml with loom-config.yaml as the canonical config reference
            description: Confirm operator-provided loom-config.yaml is committed and
              loom-config.toml is removed, not merely superseded in docs.
            status: completed
            token_estimate: small
            files:
              - loom-config.yaml
            dependencies: []
          - id: t6-3-2
            name: Update SPEC.md section 7 to reference loom-config.yaml instead of loom-config.toml
            description: Update SPEC.md section 7 so the exact manifest references
              loom-config.yaml rather than loom-config.toml.
            status: completed
            token_estimate: small
            files:
              - SPEC.md
            dependencies:
              - t6-3-1
          - id: t6-3-3
            name: Update README.md and any other doc showing a TOML config example
            description: Update README.md and any other documentation that shows the
              user-facing LOOM runtime config as TOML.
            status: completed
            token_estimate: small
            files:
              - README.md
            dependencies:
              - t6-3-1
          - id: t6-3-4
            name: Search the full repo for remaining .toml config references and fix or explicitly justify each one
            description: Fix or justify remaining TOML references; prompt-intelligence.config.toml
              and foundry-server.toml are legitimate TOML and are not part of the
              user-facing runtime config cleanup.
            status: completed
            token_estimate: small
            files:
              - SPEC.md
              - README.md
            dependencies:
              - t6-3-1
      - id: m6-4
        name: Produce a real installable release artifact
        description: A person can run one documented command to install a working
          loom binary onto PATH without cloning the repository or having Bun installed.
        status: completed
        token_estimate: large
        tasks:
          - id: t6-4-1
            name: Write install.sh that detects OS and arch, downloads the matching compiled binary, and installs it
            description: Implement an installer following the get.loom.dev-style
              deployment flow, using GitHub Releases URL patterns and a configurable
              host override.
            status: completed
            token_estimate: medium
            files:
              - install.sh
            dependencies:
              - m6-1
              - m6-2
          - id: t6-4-2
            name: Add a release build script that produces checksummed tarballs for all four platform targets
            description: Reuse existing build:linux and build:mac scripts and package
              each binary plus SHA256SUMS without inventing a new build mechanism.
            status: completed
            token_estimate: medium
            files:
              - package.json
              - scripts
            dependencies:
              - t6-4-1
          - id: t6-4-3
            name: Document a GitHub Releases publish procedure
            description: Document a manual GitHub Releases publish procedure for the
              initial installer-style release; no CI/CD is required yet.
            status: completed
            token_estimate: small
            files:
              - README.md
              - RELEASE.md
            dependencies:
              - t6-4-2
          - id: t6-4-4
            name: First-run wizard writes ~/.loom/config.yaml when missing
            description: If no ~/.loom/config.yaml exists, prompt for a backend endpoint
              and write a usable YAML config so new users do not hand-write config
              before first run.
            status: completed
            token_estimate: medium
            files:
              - src/config
              - src/index.ts
              - tests/config
            dependencies:
              - m6-1
              - m6-2
      - id: m6-5
        name: User testing guide and operator dry run
        description: USER_TESTING.md exists, matches the actual installed product, and
          has been completed in a clean environment with no repository access.
        status: completed
        token_estimate: large
        tasks:
          - id: t6-5-1
            name: Write USER_TESTING.md as a standalone guide for a first-time tester
            description: Verify every command in USER_TESTING.md against the actual
              m6-4 installer output before trusting it.
            status: completed
            token_estimate: medium
            files:
              - USER_TESTING.md
            dependencies:
              - m6-1
              - m6-2
              - m6-3
              - m6-4
          - id: t6-5-2
            name: Perform one full dry run of USER_TESTING.md without repository access
            description: Run the guide using the installer from m6-4, not bun run build,
              from a machine or directory with no repository clone.
            status: completed
            token_estimate: medium
            files:
              - USER_TESTING.md
            dependencies:
              - t6-5-1
              - m6-6
          - id: t6-5-3
            name: Fix any USER_TESTING.md step that failed or was unclear during dry run
            description: Correct any guide or product issue discovered during the clean
              dry run before handing LOOM to an outside tester.
            status: completed
            token_estimate: medium
            files:
              - USER_TESTING.md
            dependencies:
              - t6-5-2
      - id: m6-6
        name: Dry-run remediation first-run wizard, CLI input strictness, banner cleanup
        description: Fix the real compiled-binary dry-run failures found in LOOM_test.txt
          before claiming the no-repository USER_TESTING.md dry run is complete.
        exit_criterion: A real interactive session, run from a real terminal against the
          compiled binary, successfully types a backend URL at the first-run prompt and
          has it accepted, persisted to ~/.loom/config.yaml, and used for the subsequent
          session.
        status: completed
        token_estimate: xlarge
        tasks:
          - id: t6-6-1
            name: Reproduce and fix first-run wizard stdin handling in the compiled binary
            description: Root-cause and fix the difference between existing first-run
              tests and real TTY behavior in a Bun-compiled binary; verify against the
              actual compiled artifact, not only bun run src/index.ts.
            status: completed
            token_estimate: large
            files:
              - src/config/first-run.ts
              - src/index.ts
            dependencies:
              - m6-4
          - id: t6-6-2
            name: Add a real-terminal regression test for the first-run wizard
            description: Add a pty-backed or delayed subprocess regression test that
              exercises the compiled binary the way a human would, not only a direct
              function call with mocked stdin.
            status: completed
            token_estimate: medium
            files:
              - tests
            dependencies:
              - t6-6-1
          - id: t6-6-3
            name: Reject unrecognized positional arguments and unknown flags
            description: Configure the top-level CLI to reject unexpected positional
              arguments and unknown options with a clear non-zero error instead of
              silently starting a session.
            status: completed
            token_estimate: medium
            files:
              - src/index.ts
            dependencies: []
          - id: t6-6-4
            name: Remove or rename the LOOM TUI placeholder startup banner
            description: Trace the startup banner source and remove or rename leftover
              placeholder text once the active renderer path is confirmed.
            status: completed
            token_estimate: small
            files:
              - src/tui/session.ts
            dependencies: []
          - id: t6-6-5
            name: Make /agent name matching case-insensitive
            description: Accept mixed-case agent names such as /agent Tester to remove
              a confusion point observed during real user testing.
            status: completed
            token_estimate: small
            files:
              - src/tui/session.ts
            dependencies: []
          - id: t6-6-6
            name: Full clean re-run of USER_TESTING.md from a real terminal
            description: Complete the mandatory clean dry run from a real terminal using
              an installed binary via install.sh, not direct bun build output.
            status: completed
            token_estimate: large
            files:
              - USER_TESTING.md
            dependencies:
              - t6-6-1
              - t6-6-2
              - t6-6-3
              - t6-6-4
              - t6-6-5
  - id: phase-7
    name: Production release readiness
    description: Turn the installable, usable LOOM CLI into a versioned public release
      that users can trust and find support for.
    exit_criterion: LOOM v0.1.0 is tagged, published as a GitHub Release with working
      install instructions, has a changelog and license, and has a defined process for
      receiving and triaging real user feedback. Independent external testing is deferred
      to the post-UX Phase 8 release gate.
    status: completed
    token_estimate: xlarge
    milestones:
      - id: m7-1
        name: Versioning policy, changelog, and license
        description: Ensure a person reading the repository root understands the version,
          user-facing changes, license terms, and support path.
        status: completed
        token_estimate: medium
        tasks:
          - id: t7-1-1
            name: Decide and document the versioning policy and what 1.0.0 means
            description: Confirm with the operator whether the first public release is
              v0.1.0, signaling early but usable, or v1.0.0, signaling production-stable;
              document that decision and criteria for future SemVer bumps.
            status: completed
            token_estimate: small
            dependencies: []
          - id: t7-1-2
            name: Write CHANGELOG.md summarizing Phases 1-7 in user-facing terms
            description: Describe user-visible capabilities and changes rather than
              copying internal implementation history from narrative.md.
            status: completed
            token_estimate: medium
            dependencies: []
          - id: t7-1-3
            name: Add a LICENSE file
            description: Confirm the license choice with the operator before adding a
              license; do not assume one.
            status: completed
            token_estimate: small
            dependencies: []
          - id: t7-1-4
            name: Final README command and config verification
            description: Confirm every documented command and config example matches the
              actual released binary.
            status: completed
            token_estimate: medium
            dependencies:
              - t7-1-1
      - id: m7-2
        name: Pre-release security and safety review
        description: Explicitly review the expanded attack surface introduced by the
          installer and first-run wizard rather than assuming earlier safety still holds.
        status: completed
        token_estimate: large
        tasks:
          - id: t7-2-1
            name: Re-audit path-safety, shell, and git-ops tool boundaries
            description: Confirm later pipeline, sub-agent, and Foundry work did not
              introduce a way to bypass project-root path safety.
            status: completed
            token_estimate: medium
            dependencies: []
          - id: t7-2-2
            name: Audit output and records for secrets
            description: Confirm API keys, backend URLs, and config secrets are never
              logged, printed in errors, or written to narrative.md or handoff.md.
            status: completed
            token_estimate: medium
            dependencies: []
          - id: t7-2-3
            name: Review installer supply-chain protections
            description: Confirm install.sh enforces checksum verification before any
              downloaded binary is executed.
            status: completed
            token_estimate: medium
            dependencies: []
          - id: t7-2-4
            name: Confirm restrictive config file permissions
            description: Confirm ~/.loom/config.yaml is written with restrictive file
              permissions when it may contain an API key.
            status: completed
            token_estimate: small
            dependencies: []
      - id: m7-3
        name: Publish the official v1 GitHub Release
        description: Follow RELEASE.md end to end for a real immutable tagged release,
          not a test or staging run.
        status: completed
        token_estimate: large
        tasks:
          - id: t7-3-1
            name: Follow RELEASE.md steps 1-6 for the chosen version
            description: Build and publish the real version tag selected in t7-1-1 after
              versioning and security milestones are complete.
            status: completed
            token_estimate: medium
            dependencies:
              - m7-1
              - m7-2
          - id: t7-3-2
            name: Verify the published installer path without repository access
            description: Follow RELEASE.md step 7 against the live GitHub Release URL;
              Phase 6 local or staging artifact evidence is not a substitute.
            status: completed
            token_estimate: medium
            dependencies:
              - t7-3-1
          - id: t7-3-3
            name: Complete post-release checks
            description: Complete RELEASE.md step 8 against the published release.
            status: completed
            token_estimate: small
            dependencies:
              - t7-3-2
      - id: m7-4
        name: Real user feedback loop
        description: Define where users report problems and how incoming reports are
          triaged before the first external report arrives.
        status: completed
        token_estimate: large
        tasks:
          - id: t7-4-1
            name: Add GitHub issue templates
            description: Add separate bug report and feature request templates.
            status: completed
            token_estimate: small
            dependencies:
              - m7-3
          - id: t7-4-2
            name: Document the external contribution policy
            description: Write CONTRIBUTING.md if external contributions are in scope;
              otherwise explicitly document that they are not yet accepted.
            status: completed
            token_estimate: small
            dependencies:
              - m7-3
  - id: phase-8
    name: Full-screen Ink TUI and look-and-feel polish (0.1.1)
    description: UX and interaction-model release following completed Phase 7. Replace
      the readline loop with a persistent Ink application and deliver the associated
      look-and-feel improvements without adding backend capability.
    exit_criterion: LOOM runs as a persistent, live-redrawing full-screen terminal
      application matching the interaction feel of comparable tools (instant keybindings,
      streaming output, pinned input, no reprinted frames), every polish item is implemented
      and verified in a real terminal by the operator, independent external testing and
      sanitized-findings remediation are complete, and v0.1.1 is released.
    status: in_progress
    token_estimate: xlarge
    milestones:
      - id: m8-1
        name: Rebuild session.ts as a persistent Ink render tree
        description: Developer-owned core architectural change. A single Ink render()
          call owns the terminal for the session lifetime; commands update the existing
          frame instead of reprinting the tab strip or status bar as new blocks.
        status: in_progress
        token_estimate: xlarge
        tasks:
          - id: t8-1-1
            name: Replace the readline loop in src/tui/session.ts with a top-level Ink render() app
            description: Preserve all existing agent-switching, tool-call, and conversation-state
              logic; this is a rendering-layer rewrite, not a behavior change. AgentContext,
              SessionManager, and handoff triggers must work identically to today.
            status: completed
            token_estimate: large
            dependencies: []
          - id: t8-1-2
            name: Add useInput-based keybindings with instant Tab agent cycling
            description: Use Ink input handling so Tab cycles agents immediately without
              requiring Enter.
            status: completed
            token_estimate: medium
            dependencies:
              - t8-1-1
          - id: t8-1-3
            name: Stream assistant responses into component state token-by-token
            description: Update live component state for each token instead of printing
              only a completed assistant response block.
            status: in_progress
            token_estimate: medium
            dependencies:
              - t8-1-1
          - id: t8-1-4
            name: Use Static scrollback with a live turn region and pinned input bar
            description: Render completed scrollback through Static while retaining a live
              region for the in-progress turn and a pinned input bar.
            status: pending
            token_estimate: medium
            dependencies:
              - t8-1-1
          - id: t8-1-5
            name: Handle resize, exits, and non-TTY fallback
            description: Handle terminal resize and Ctrl+C or Esc exit without regressing
              non-TTY input. Piped and scripted usage, CI, and --prompt one-shot mode must
              continue to work without a real terminal.
            status: pending
            token_estimate: medium
            dependencies:
              - t8-1-1
      - id: m8-2
        name: Slash command popup list, replacing tab-only discovery
        description: Developer-owned work after m8-1. Typing / as the first input character
          shows a live-filtered, navigable list of matching commands and agents near the
          input line so users do not need to know exact syntax or cycle blindly with Tab.
        status: pending
        token_estimate: large
        tasks:
          - id: t8-2-1
            name: Show a popup list of available slash commands as soon as / is typed
            description: Include /agent with one entry per built-in and installed agent,
              /tab, /agents, /exit, /quit, and future slash commands. Render the popup
              inline in the Ink tree, not in a separate terminal region.
            status: pending
            token_estimate: medium
            dependencies: []
          - id: t8-2-2
            name: Filter the slash command list live
            description: Filter matching popup entries as characters are typed after /.
            status: pending
            token_estimate: small
            dependencies:
              - t8-2-1
          - id: t8-2-3
            name: Navigate and select slash command popup entries
            description: Navigate with Up and Down, and select with Enter or a mouse click
              where supported. Tab may remain an alternate accept, but Enter or click is
              primary so discovery never relies on invisible Tab cycling.
            status: pending
            token_estimate: medium
            dependencies:
              - t8-2-1
          - id: t8-2-4
            name: Close the slash command popup with Esc
            description: Esc closes the popup without submitting or clearing typed input.
            status: pending
            token_estimate: small
            dependencies:
              - t8-2-1
      - id: m8-3
        name: Distinguish thinking, tool-running, and idle states
        description: Developer-owned live feedback work after m8-1 that makes thinking,
          tool execution, and idle states visibly distinct.
        status: pending
        token_estimate: medium
        tasks:
          - id: t8-3-1
            name: Add a thinking indicator before the first streamed token
            description: Show a spinner or animated dots immediately when a prompt is
              submitted and before the first streamed token arrives.
            status: pending
            token_estimate: small
            dependencies: []
          - id: t8-3-2
            name: Add a distinct tool-running indicator
            description: Show tool execution with a visual state separate from the thinking
              indicator.
            status: pending
            token_estimate: medium
            dependencies:
              - t8-3-1
      - id: m8-4
        name: Scrollback hierarchy, markdown rendering, and file-write diffs
        description: Developer-owned reading-density and hierarchy work after m8-1.
        status: pending
        token_estimate: xlarge
        tasks:
          - id: t8-4-1
            name: Visually recede completed turns
            description: Dim or otherwise recede completed turns so the current exchange
              remains the clear focus.
            status: pending
            token_estimate: small
            dependencies: []
          - id: t8-4-2
            name: Render styled markdown and syntax-highlighted code blocks
            description: Render real syntax-highlighted code blocks and styled bold text
              and headers. Evaluate an existing Ink-compatible terminal Markdown library
              before hand-rolling one.
            status: pending
            token_estimate: large
            dependencies: []
          - id: t8-4-3
            name: Show file writes as red and green diffs
            description: Replace File write ok with +/- line diffs. This high-impact trust
              signal requires the file-writer tool to capture before and after content,
              not only a success boolean.
            status: pending
            token_estimate: large
            dependencies: []
      - id: m8-5
        name: Multi-line input and command history
        description: Developer-owned input-experience work after m8-1.
        status: pending
        token_estimate: large
        tasks:
          - id: t8-5-1
            name: Support multi-line input with submit and newline hints
            description: Support Shift+Enter or an equivalent newline action and visibly
              distinguish it from prompt submission.
            status: pending
            token_estimate: medium
            dependencies: []
          - id: t8-5-2
            name: Add Up-arrow command history
            description: Reimplement the prior readline behavior so Up recalls previous
              prompts.
            status: pending
            token_estimate: medium
            dependencies: []
      - id: m8-6
        name: Surface Session Continuity, recall, and sub-agent activity
        description: Developer-owned differentiator-visibility work after m8-1.
        status: pending
        token_estimate: large
        tasks:
          - id: t8-6-1
            name: Show a status-line notice when a handoff triggers
            description: Make handoff generation visible instead of silently writing it
              to disk.
            status: pending
            token_estimate: small
            dependencies: []
          - id: t8-6-2
            name: Indicate when inject or recall uses prior-session context
            description: Add a visual signal whenever inject or recall pulls in context
              from a prior session.
            status: pending
            token_estimate: medium
            dependencies: []
          - id: t8-6-3
            name: Surface sub-agent activity in a live tray
            description: Add the small live sub-agent activity tray from the original
              design specification; this activity is invisible in the shipped product.
            status: pending
            token_estimate: medium
            dependencies: []
      - id: m8-7
        name: Cursor, color-coded results, and end-of-turn summaries
        description: Developer-owned small polish work after m8-1.
        status: pending
        token_estimate: medium
        tasks:
          - id: t8-7-1
            name: Add a real blinking input cursor
            description: Display a real blinking cursor in the input line.
            status: pending
            token_estimate: small
            dependencies: []
          - id: t8-7-2
            name: Color-code tool results by outcome
            description: Render success in green and failures in amber or red instead of
              plain text.
            status: pending
            token_estimate: small
            dependencies: []
          - id: t8-7-3
            name: Set apart the Developer end-of-turn summary
            description: Visually distinguish the what-changed summary that the Developer
              agent already produces.
            status: pending
            token_estimate: small
            dependencies: []
      - id: m8-8
        name: Independent external testing and findings remediation
        description: External release gate after m8-1 through m8-7 and before m8-9. A
          tester who is not the operator and lacks deep project context runs USER_TESTING.md
          against the completed full-screen UX; all resulting reports are sanitized before
          they are recorded, triaged, and addressed. Deferred Phase 7 testing remains
          incomplete until this milestone is actually performed.
        status: pending
        token_estimate: large
        tasks:
          - id: t8-8-1
            name: Run USER_TESTING.md with an independent external tester
            description: A tester who is not the operator and has no deep project context
              must run the guide against the completed full-screen UX after m8-1 through
              m8-7. Record only sanitized findings and do not substitute operator testing.
            status: pending
            token_estimate: large
            dependencies:
              - m8-1
              - m8-2
              - m8-3
              - m8-4
              - m8-5
              - m8-6
              - m8-7
          - id: t8-8-2
            name: Triage and address all sanitized external-test findings
            description: Review every sanitized finding, fix product or guide defects, and
              document any explicit disposition before release work begins.
            status: pending
            token_estimate: large
            dependencies:
              - t8-8-1
      - id: m8-9
        name: Real-terminal verification and 0.1.1 release
        description: Tester-led final milestone after m8-1 through m8-8. The operator must
          personally use the full-screen TUI in a real terminal, confirm every Phase 8 item,
          and publish v0.1.1 by following RELEASE.md exactly as for v0.1.0.
        status: pending
        token_estimate: xlarge
        tasks:
          - id: t8-9-1
            name: Run full regression and real end-to-end Phase 8 verification
            description: Run bun run typecheck, bun run lint, and bun test, plus a real
              end-to-end session covering every m8-1 through m8-7 item. Begin only after
              independent external testing and all sanitized findings are addressed.
            status: pending
            token_estimate: large
            dependencies:
              - m8-8
          - id: t8-9-2
            name: Update CHANGELOG.md for the v0.1.1 interaction-model change
            description: State plainly that v0.1.1 changes the core interaction model and
              is not a silent patch.
            status: pending
            token_estimate: small
            dependencies:
              - t8-9-1
          - id: t8-9-3
            name: Bump package.json version to 0.1.1
            description: Set package.json to the Phase 8 release version.
            status: pending
            token_estimate: small
            dependencies:
              - t8-9-1
          - id: t8-9-4
            name: Follow RELEASE.md steps 1-8 for v0.1.1
            description: Publish v0.1.1, including unauthenticated live-install verification
              from outside the repository.
            status: pending
            token_estimate: medium
            dependencies:
              - t8-9-2
              - t8-9-3
          - id: t8-9-5
            name: Operator dry run from the published v0.1.1 binary
            description: The operator must run the full-screen TUI from the published
              v0.1.1 binary, not local build artifacts.
            status: pending
            token_estimate: medium
            dependencies:
              - t8-9-4
