- **Goal & status** — Phase 8 UX work is active at priority milestone `m8-2` / `t8-2-4`; Phase 8 is merged with 21 tasks remaining, `m8-1` is complete, and v0.1.1 is not released.
- **Completed work** — Completed and committed `t8-2-3` as `faec558`: Up/Down now move a visible wraparound popup selection, Enter submits the selected command, bare Tab still cycles agents, and compiled PTY verification, typecheck, lint, all 243 tests, and review passed.
- **Failed attempts** — The USER_TESTING documentation subagent placed navigation where no popup was open and omitted the expected-result change, so that bounded edit was corrected directly; Ink 7.1 exposes no mouse-event API, so conditional click selection is unsupported without adding out-of-scope capability. The untracked `.loom/SPEC.md` duplicate still requires operator-controlled reconciliation, and repository status remains dirty only from pre-existing operator/support files.
- **Branch name** — main
- **Next action** — Read the current `SessionApp` exit/input ordering and focused tests, then implement `t8-2-4` so Escape closes an open slash popup without submitting or clearing input while Escape outside the popup still exits.
