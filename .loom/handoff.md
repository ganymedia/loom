- **Goal & status** — Phase 7 security review is active at `m7-2` / `t7-2-4`; `m6-6` is merged with 0 tasks remaining, and Phase 7 is merged with 8 tasks remaining.
- **Completed work** — Completed and committed `t7-2-3` as `7178793`; `install.sh` now requires exact SHA-256 manifest verification before destination-local staging, tests cover success plus mismatch/missing/duplicate failures, independent re-review passed, and full verification passed with `bun run typecheck`, `bun run lint`, and `bun test` (221 passing tests).
- **Failed attempts** — The first checksum implementation reused global POSIX-shell function variables and fetched the manifest twice, then focused tests exposed the collision; the docs agent returned no report; initial review overlooked existing missing-entry coverage but identified destination-staging hardening that was implemented; git status is not clean only because pre-existing operator files remain unstaged.
- **Branch name** — main
- **Next action** — Read `src/config/first-run.ts` and trace every `~/.loom/config.yaml` write path and resulting file mode for `t7-2-4`.
