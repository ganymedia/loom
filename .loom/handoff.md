- **Goal & status** — Phase 8 UX work is active at priority milestone `m8-2` / `t8-2-3`; Phase 8 is merged with 22 tasks remaining, `m8-1` is complete, and v0.1.1 is not released.
- **Completed work** — Completed and committed `t8-2-2` as `8fe9eb1`: popup entries now filter live with case-insensitive executable-prefix matching, unmatched prefixes retain an explicit no-match state, and compiled PTY verification, typecheck, lint, and all 241 tests passed.
- **Failed attempts** — The USER_TESTING documentation subagent again returned no usable change, so the bounded edit was applied directly. Review returned no applicable findings and incorrectly described the existing filter as case-sensitive, cited a nonexistent timeout-based unit test, and requested Escape behavior reserved for `t8-2-4`. The untracked `.loom/SPEC.md` duplicate still requires operator-controlled reconciliation, and repository status remains dirty only from pre-existing operator/support files.
- **Branch name** — main
- **Next action** — Trace Ink 7 keyboard and mouse support, then design `t8-2-3` selected-index state so Up/Down navigation and Enter acceptance are primary without regressing bare-Tab agent cycling.
