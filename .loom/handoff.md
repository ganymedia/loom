- **Goal & status** — Phase 8 UX work is active at `m8-1` / `t8-1-1`; Phase 7 is complete with 0 tasks remaining, Phase 8 is merged with 29 tasks remaining, and v0.1.1 is not released.
- **Completed work** — Committed the no-external-contributions policy as `c6a96af`, deferred independent testing to a mandatory post-UX pre-release gate and closed Phase 7 as `2c90d3d`, then promoted and merged the Phase 8 addendum as `338d118`; typecheck, lint, planner checks, and all 224 tests passed.
- **Failed attempts** — `/home/user/projects/loom` and the earlier suggested home-level checkout were not mounted; the active checkout is `/home/austin.pol@jcdx.coi/projects/loom`. Independent testing was not run or claimed complete because the operator is not ready to expose the pre-Phase-8 UX; global git status remains dirty only from pre-existing operator files.
- **Branch name** — main
- **Next action** — Read `src/tui/session.ts` and its session tests, then design the minimal persistent Ink render-tree boundary for `t8-1-1` while preserving existing behavior and explicit non-TTY paths.
