- **Goal & status** — Phase 8 UX work is active at priority milestone `m8-2` / `t8-2-2`; Phase 8 is merged with 23 tasks remaining, `m8-1` is complete, and v0.1.1 is not released.
- **Completed work** — Completed and committed `t8-2-1` as `c8b594d`: typing `/` first renders an inline themed popup for every current built-in agent plus `/tab`, `/agents`, `/exit`, and `/quit`; one typed registry now feeds popup and parser literals, and typecheck, lint, compiled PTY verification, all 239 tests, and review passed.
- **Failed attempts** — The registry scout needed three attempts before returning a partial report, and the USER_TESTING documentation subagent returned no usable result, so the bounded documentation edit was applied directly. Reconnaissance confirmed there is no installed-agent runtime registry; adding one would violate Phase 8 scope, so no unsupported entries were fabricated. The untracked `.loom/SPEC.md` duplicate still requires operator-controlled reconciliation, and repository status remains dirty only from pre-existing operator/support files.
- **Branch name** — main
- **Next action** — Add a pure, tested `t8-2-2` filter over the typed popup entries, then pass `SessionApp` input into it so the visible list updates on every character after `/`.
