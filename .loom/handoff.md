- **Goal & status** — Phase 8 UX work is active at `m8-1` / `t8-1-5`; Phase 8 is merged with 25 tasks remaining, and v0.1.1 is not released.
- **Completed work** — Completed and committed `t8-1-4` as `37137fa`: completed output now uses Ink `<Static>` above the live tab/assistant/status/input frame; typecheck, lint, the compiled PTY regression, all 235 tests, and focused plan/render tests passed without changing controller or non-TTY behavior.
- **Failed attempts** — The initial render test failed against the old live-tree output as intended; first lint and typecheck runs exposed only import/formatting issues and Ink 7's mutable-array type requirement, resolved with exact formatting and a boundary copy. The independent review subagent was cancelled after producing no findings. The untracked `.loom/SPEC.md` duplicate still requires operator-controlled reconciliation, and repository status remains dirty only from pre-existing operator/support files.
- **Branch name** — main
- **Next action** — Read the current Ink session shutdown and input paths, then implement `t8-1-5` resize, Ctrl+C/Esc exit, and explicit non-TTY fallback handling without changing `--prompt`, piped-input, or CI behavior.
