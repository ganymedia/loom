- **Goal & status** — Phase 8 UX work is active at priority milestone `m8-2` / `t8-2-1`; Phase 8 is merged with 24 tasks remaining, `m8-1` is complete, and v0.1.1 is not released.
- **Completed work** — Completed and committed `t8-1-5` and `m8-1` as `c287821`: confirmed-TTY sessions handle resize and clean Escape/Ctrl+C exits with alternate-screen restoration, real piped stdin works across file/FIFO/socket transports, and scripted/CI/`--prompt` paths remain non-TTY; typecheck, lint, focused lifecycle tests, and all 236 tests passed.
- **Failed attempts** — The first resize harness used byte-identical resize output as its oracle and deadlocked until termination, so it was replaced with a post-resize agent-state render check; Bun child stdin proved socket-backed rather than FIFO-backed, requiring explicit socket detection. Scout, review, and USER_TESTING documentation subagents each initially returned no usable report; review follow-up produced two applicable cleanup findings and one invalid single-threaded queue-race claim. The untracked `.loom/SPEC.md` duplicate still requires operator-controlled reconciliation, and repository status remains dirty only from pre-existing operator/support files.
- **Branch name** — main
- **Next action** — Trace existing slash-command parsing and built-in/installed agent registries, then design the `t8-2-1` popup data model before editing `SessionApp`.
