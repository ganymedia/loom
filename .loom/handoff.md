- **Goal & status** — Phase 8 UX work is active at milestone `m8-4` / `t8-4-1`; Phase 8 is merged with 18 tasks remaining, milestones `m8-1` through `m8-3` are complete, and v0.1.1 is not released.
- **Completed work** — Completed and committed `t8-3-2` as `bbf5e2d`: all built-in agents now report renderer-only tool start/finish count events, and persistent Ink shows a distinct animated `Running N tool(s)` state without exposing tool data; compiled PTY verification, typecheck, lint, all 251 tests, planner validation, and review passed.
- **Failed attempts** — The reconnaissance subagent and USER_TESTING documentation subagent returned no report/change; the first compiled fixture run referenced a nonexistent test helper and passed after replacing it with `Response.json`; review requested fixes for pre-existing Promise.all concurrency, project-root injection, and placeholder summaries outside this task. The untracked `.loom/SPEC.md` duplicate still requires operator-controlled reconciliation, and repository status remains dirty only from pre-existing operator/support files.
- **Branch name** — main
- **Next action** — Read the current `<Static>` scrollback rendering and output-state representation before implementing `t8-4-1` visual recession of completed turns without changing stored output or non-TTY rendering.
