- **Goal & status** — Phase 8 UX work is active at `m8-1` / `t8-1-4`; Phase 8 is merged with 26 tasks remaining, and v0.1.1 is not released.
- **Completed work** — Completed and committed `t8-1-3` as `658390d`: persistent TTY sessions now use bounded real OpenAI-compatible SSE, safe built-in-agent text projection, split-secret streaming redaction, and a live assistant region while non-TTY/`--prompt` behavior remains completed JSON; typecheck, lint, all 234 tests, and security re-review passed.
- **Failed attempts** — Reconnaissance proved true streaming required new transport/agent capability, so implementation paused until the operator explicitly authorized a Phase 8 scope exception; initial security review correctly found missing SSE buffer bounds, which were added, while its split-secret and envelope-leak findings were disproved by the current algorithms and tests. The untracked `.loom/SPEC.md` duplicate still requires operator-controlled reconciliation, and repository status remains dirty only from pre-existing operator/support files.
- **Branch name** — main
- **Next action** — Implement `t8-1-4` by moving completed output into Ink `<Static>` while retaining the streaming live assistant region and pinned input/status areas.
